apiVersion: v1
kind: Service
metadata:
  name: vogelgrippe-ad-service
spec:
  ports:
    - targetPort: 8082
      port: 80
  type: ClusterIP
  selector:
    app: vogelgrippe-ad-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: vogelgrippe-ad-service
spec:
  selector:
    matchLabels:
      app: vogelgrippe-ad-service
  template:
    metadata:
      labels:
        app: vogelgrippe-ad-service
    spec:
      containers:
        - name: ad-service
          image: vogelgrippe-ad-service
          ports:
            - containerPort: 8082
          env:
            - name: SERVER_PORT
              value: "8082"
            - name: JAEGER_SERVICE_NAME
              value: "vogelgrippe-ad-service"
            - name: JAEGER_AGENT_HOST
              value: "jaeger-agent" # change depending on your jaeger deployment
            - name: JAEGER_SAMPLER_TYPE
              value: "const"
            - name: JAEGER_SAMPLER_PARAM
              value: "1"



